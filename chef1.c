#include<stdio.h>
unsigned int fib(int n)
{
 return fib(n-1)+fib(n-2);
}
int main()
{
 int t;
 scanf("%d",&t);
 for(i=0;i<t;i++)
 {
  scanf("%u%u",&a[i],&b[i]);
  for(i=a[i];i<b[i];i++)
   {
    fib(a[i] 
  
